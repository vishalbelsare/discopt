"""Developer-only tooling for the discopt source tree.

These commands (literature scanner, adversary tester, the search/report helpers
they call, and the ``claude`` CLI bridge) only make sense when run from inside
a discopt checkout. They are exposed via the ``discopt-dev`` console script
rather than the user-facing ``discopt`` CLI.
"""
